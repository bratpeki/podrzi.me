```sh
mvn install clean compile package
```

`mvn`     - maven komanda
`install` - preuzimanje dependency biblioteka
`clean`   - brisanje starih .class fajlova
`compile` - kompilacija
`package` - pravi JAR u target folderu

```sh
java -jar target/[...].jar
```
